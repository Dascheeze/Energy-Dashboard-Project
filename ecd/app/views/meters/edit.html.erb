<h1>Editing meter</h1>

<% form_for(@meter) do |f| %>
  <%= f.error_messages %>

 	<p>
		<!-- Makes a dropdown and puts the building type in each select value -->
		<% buildingIDs = Building.find(:all, :order => :id) %>
		Select <%= f.label :building_id %><br /> 
		<%= f.collection_select :building_id, buildingIDs, :id, :name %>
	</p>
  <p>
    <%= f.label :modbus_address %><br />
    <%= f.text_field :modbus_address %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @meter %> |
<%= link_to 'Back', meters_path %>